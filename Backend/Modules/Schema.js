import mongoose from "mongoose";

const adminSchema = new mongoose.Schema({

});

const userSchema = new mongoose.Schema({

});

const instructorSchema = new mongoose.Schema({

});

export { adminSchema, userSchema, instructorSchema };